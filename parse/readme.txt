Parses a formatted file; see address-sample for an example.
Counts number of failed login attempts as per IP addr.
If IP addr occurence is greater than 10, it will display as:

# of Attempts, IP Addr., Location
